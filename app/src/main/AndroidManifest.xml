<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.RECEIVE_LOCKED_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
 
    <application
        android:label="BackgroundWorkAround"
        android:icon="@drawable/ic_launcher"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">

        <receiver android:name=".StartReceiver" 
            android:process=":rec1"
            android:directBootAware="true"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
            
            <intent-filter>
                <action android:name="background.work.around.START" />
            </intent-filter>
        </receiver>

        <activity android:name=".MainActivity"
            android:exported="true"
            android:directBootAware="true"
            android:process=":act0"
            android:showWhenLocked="true"
            android:screenOrientation="portrait"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".WatcherService"
            android:directBootAware="true"
            android:process=":serv1"
            android:stopWithTask="false"
            android:exported="false" />

        <service 
            android:name=".WatcherService2"
            android:directBootAware="true"
            android:foregroundServiceType="specialUse"
            android:process=":serv2"
            android:stopWithTask="false"
            android:exported="false">
       <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_TYPE"
        android:value="Universal foreground service"/>
        </service>

    </application>
</manifest>
